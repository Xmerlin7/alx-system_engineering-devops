#!/usr/bin/env bash
# This script displays a list of currently running processes

# Display header
echo "USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND"

# Use ps command to list all processes in a user-oriented format with hierarchy
ps aux --forest
